global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: asi-server
    metrics_path: /api/metrics
    static_configs:
      - targets: ['asi-server:3000']
